## setup

```
firebase functions:config:set hasura.url="https://react-query-spacex-firebase.hasura.app/v1/graphql"
firebase functions:config:set hasura.admin_secret="{hasura Env vars HASURA_GRAPHWL_ADMIN_SECRET}"
```
